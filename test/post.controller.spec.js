/*
import { resolvePlugin } from '@babel/core';
import { createPost } from '../models/crud.js';
import '../__mocks__/functionMock.js';
import firestoremock from 'firebase-mock/src/firebase';
global.firebase = new MockFirebase(fixtureData)

describe('agregar post', () => {
  it('Debería agregar un post'),
  () => createPost('hola mundo').then((data) => {
    expect(data).toBe('el post fue agregado');
  });
});
*/
